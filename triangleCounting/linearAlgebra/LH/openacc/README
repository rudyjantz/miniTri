Does not build, because the top-level loop can't decorate the memset routine.
Line 118 is where the error occurs. Commenting out that std::map definition
allows it to build (though of course it's an incorrect binary).
